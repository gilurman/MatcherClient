import { CandidateJobPipe } from './candidate-job.pipe';

describe('CandidateJobPipe', () => {
  it('create an instance', () => {
    const pipe = new CandidateJobPipe();
    expect(pipe).toBeTruthy();
  });
});
